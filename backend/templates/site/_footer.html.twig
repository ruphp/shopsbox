<footer class="footer site-footer">
    <div class="wrap footer-grid">
        <div class="footer-about">
            <strong>ShopsBox</strong>
            <p>Конструктор интернет-магазина под ключ: каталог, админка, блоки страниц, домен, SSL и сопровождение.</p>
            <span>© {{ 'now'|date('Y') }} ShopsBox.ru</span>
        </div>
        <nav class="footer-links" aria-label="Продукт">
            <strong>Продукт</strong>
            <a href="{{ path('site_home') }}#builder">Конструктор страниц</a>
            <a href="{{ path('site_home') }}#operations">Сопровождение</a>
            <a href="{{ path('site_home') }}#migration">Перенос магазина</a>
            <a href="{{ path('storefront_products', {storeSlug: 'demo-store'}) }}">Пример каталога</a>
        </nav>
        <nav class="footer-links" aria-label="Документы">
            <strong>Документы</strong>
            <a href="{{ path('site_static', {page: 'privacy'}) }}">Политика конфиденциальности</a>
            <a href="{{ path('site_static', {page: 'terms'}) }}">Пользовательское соглашение</a>
            <a href="{{ path('site_static', {page: 'offer'}) }}">Публичная оферта</a>
            <a href="{{ path('site_static', {page: 'contacts'}) }}">Контакты</a>
        </nav>
        <nav class="footer-links" aria-label="Техническая информация">
            <strong>Технически</strong>
            <a href="{{ path('site_static', {page: 'status'}) }}">Статус сервиса</a>
            <a href="{{ path('site_sitemap') }}">Sitemap.xml</a>
            <a href="{{ path('site_robots') }}">Robots.txt</a>
        </nav>
    </div>
</footer>
